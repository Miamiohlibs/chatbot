# ================================================================
# ALPHA INTERNAL RELEASE - GOOGLE CSE CONFIGURATION
# ================================================================
# This configuration caps Google Custom Search Engine usage to
# 300 queries per day for alpha internal testing.
#
# APPLY THESE CHANGES TO .env BEFORE ALPHA DEPLOYMENT
# ================================================================

# Google Site Search - ENABLED for alpha
DISABLE_GOOGLE_SITE_SEARCH=0

# Alpha Daily Limit - Conservative cap for internal testing
# Recommended: 300 queries/day for ~20-30 internal testers
# This prevents runaway costs during alpha validation
GOOGLE_SEARCH_DAILY_LIMIT=300

# Cache TTL - 7 days (604800 seconds)
# High TTL reduces external API calls after initial queries
# Cache hit rate expected: 60-80% after first few days
GOOGLE_SEARCH_CACHE_TTL_SECONDS=604800

# ================================================================
# COST ESTIMATE
# ================================================================
# Daily Queries: 300
# External Calls (with cache): ~90-120/day (70% cache hit rate)
# Estimated Cost: $0-1.50/day or $0-45/month
# With free tier (100 queries/day): $0-13.50/month
#
# Note: Actual costs depend on Google CSE pricing tier and
# whether free tier is available for your API key.
# ================================================================

# ================================================================
# MONITORING RECOMMENDATIONS
# ================================================================
# 1. Check backend logs daily for Google CSE tool invocations
# 2. Monitor cache hit rate (should increase over time)
# 3. Track daily external call count vs. limit
# 4. Adjust limit if alpha usage patterns differ from estimate
# ================================================================

# ================================================================
# ROLLBACK TO UNLIMITED (if needed during alpha)
# ================================================================
# GOOGLE_SEARCH_DAILY_LIMIT=10000
# ================================================================
